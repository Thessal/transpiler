* Signature
ts_mean: Signal<Float> (signal: Signal<Float>, lookback: Int)

* Arguments
signal: original tabular data
lookback: the amount of periods to average

* Descriptions
The signal is smoothed by averaging for the given amount of periods.
Averaged signal is often used as a baseline signal.